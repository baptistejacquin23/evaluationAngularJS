'use strict';

describe('myApp.meilleurrestaurant module', function() {

  beforeEach(module('myApp.meilleurrestaurant'));

  describe('meilleurrestaurant controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});